// Variables as strings and integers and floats

var text = "This is a string"; // name it anything, not just 'text'
var num = 99;

console.log(text);
console.log(num);

num = "num is now a string also!"
console.log(num);

//Add string variables together
console.log(text + " and " + num);